<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed? ? "Tarefa concluída" : "Tarefa pendente" %></p>
<br>
<%= link_to "Edit", task_edit_path(@task) %>
<br>
<%= link_to "Home", tasks_path %>
<br>
<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
